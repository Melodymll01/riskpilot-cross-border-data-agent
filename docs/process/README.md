# RagDataOut Process Index

本文档记录 RagDataOut 每一步开发过程，用于复盘、代码评审与面试展示。
每步对应一个 `step_NNN_<topic>.md`，README 本身只维护索引大表。

状态枚举：`Pending` / `In Progress` / `Done` / `Blocked` / `Needs Real Service`。

| Step | Module | Status | Main Files | Test Command | Result | Conclusion | Next |
|---|---|---|---|---|---|---|---|
| [001](step_001_design_v1_freeze.md) | Design v1.1 Freeze | Done | `docs/experiment_v1.md`, `docs/decisions/ADR-001..008.md`, `docs/architecture/overview.md` | — (doc only) | 8 ADR + ~1550-line spec 冻结 | 对话式 Copilot + GitHub OAuth + 匿名双轨 + 4 层架构方案定稿；7 PR 拆分明确 | Step 002 工程基建 |
| [002](step_002_engineering_scaffold.md) | Engineering Scaffold (PR-1) | Done | `requirements-dev.txt`, `pytest.ini`, `pyproject.toml`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml`, `Makefile`, `docs/README.md` | `pip install -r requirements-dev.txt && pytest -q` | 未运行（待 step_004 启动前建立基线） | dev 依赖 / lint / type / format / pre-commit / CI / Makefile / 文档骨架就位；不动业务代码 | Step 003 gitignore 审查 |
| [003](step_003_gitignore_audit.md) | .gitignore Engineering Audit | Done | `.gitignore` | `git check-ignore -v docs/...` | 4 个 docs 文件均未被忽略 | 补齐 Python 标配缺项（.tox/.nox/.hypothesis/coverage.lcov/*.prof/.envrc/.python-version/*.orig/*.rej）；显式声明 docs/decisions / docs/process / docs/architecture / evaluations/**/datasets 为受跟踪 | Step 004 测试基线 |
| [004](step_004_baseline.md) | Test & Lint Baseline | Done | — (env only) | `pytest -q --no-cov` + `ruff check . --statistics` | **88 passed** / 437 ruff errors（347 auto-fixable） | venv 补齐（chromadb 1.5.9 用 binary-only wheel）；存量绿基线 88/88；ruff 高频项 UP006(202)/I001(54)/F401(44) 留给后续模块 PR 清理 | Step 005 PR-2 domain 层 |
| [005](step_005_pr2_domain_layer.md) | PR-2 Domain Layer | Done | `domain/{__init__,errors,models,ports}.py`, `tests/domain/test_models.py` | `pytest -q --no-cov` + `ruff check domain tests/domain` + `mypy domain` | **123 passed**（+35）/ ruff 0 / mypy 0 | 11 models + 9 Ports + 14 errors 全部落地；frozen + extra=forbid + JSON round-trip 不变量验证通过；不动业务代码、零回归 | Step 006 PR-3 infra 层 + 测试基建 |
| [006](step_006_infra_layer.md) | PR-3 Infra Layer + Test Fakes | Done | `infra/{storage,chat,search,web,evidence}/*`, `tests/{fakes,infra}/*`, `requirements.txt` | `pytest -q --no-cov` + `ruff check infra tests/infra tests/fakes` + `mypy infra` | **175 passed**（+52）/ ruff 0 / mypy 0 | 7 个 Port 适配器（SqliteUserRepo/SqliteTaskRepo/OpenAIChat/Embedder/HybridRetriever/DuckDuckGo/MockEvidence）+ 7 个 Fake 全部落地；适配器 isinstance 契约校验通过；老 API 端到端 0 回归；requirements 与 venv 对齐 | Step 007 PR-4 Auth 层（GitHub OAuth + JWT + 匿名） |
| [007](step_007_pr4_auth_layer.md) | PR-4 Auth Layer | Done | `infra/auth/*`, `tests/infra/test_{jwt_issuer,anonymous,github_oauth,auth_service,fake_auth}.py`, `tests/fakes/fake_auth.py` | `pytest -q --no-cov` + `ruff check infra/auth tests/infra tests/fakes/fake_auth.py` + `mypy infra/auth` | **224 passed**（+45）/ ruff 0 / mypy 0 | `AuthService` 组合 `JwtIssuer`+`GitHubOAuthProvider`+`AnonymousProvider`，state 自管（防 CSRF+replay+TTL），JWT 注入 clock 自校 exp，GitHub HTTP 用 `responses` 隔离；FakeAuth/FakeOAuthProvider 双层 Fake 就位 | Step 008 PR-5 App/Use-case 层 |
| [008](step_008_pr5_app_layer.md) | PR-5 App Layer (DI + Use Cases, no Agent) | Done | `app/{container,factories}.py`, `app/use_cases/{auth_login,task_management,ingest,run_query}.py`, `tests/app/*`, `config.py` (+6 fields) | `pytest -q --no-cov` + `ruff check app tests/app` + `mypy app` | **264 passed**（+40）/ ruff 0 / mypy 0（app 内） | `AppContainer` 装 8 Port + 4 use case；SQLite 单连接池共享；use case 强制 owner_id 隔离；保留 `RunQueryUseCase` 为占位待 Step 009 Agent 替换；不拆 config.py（避免动 13+ import 处） | Step 009 PR-5b Agent + Tools |
| [009](step_009_pr5b_agent_layer.md) | PR-5b Agent Layer (Copilot + Tools + RunCopilot) | Done | `app/agent/{events,decision,tools,copilot}.py`, `app/use_cases/run_copilot.py`, `app/container.py` (+3 fields), `tests/app/agent/*`, `tests/app/test_run_copilot.py` | `pytest -q` + `ruff check app tests/app` + `mypy app` | **337 passed**（+73）/ ruff 0 / mypy 0（app 内） | `ComplianceCopilotAgent` ReAct 主循环（默认 6 步）+ `AgentEvent` 9 类型流式事件 + `ToolSpec` 声明式工具注册（4 工具：search_law/search_user_docs/web_search/evidence_judge）+ LLM JSON 决策协议（保持 ChatPort 纯文本签名）+ owner_id 强制注入防越权 + 全软失败兜底（工具异常/解析错误/未知工具/max_steps） | Step 010 PR-6 API 路由 + SSE |
| [010](step_010_pr6_api_layer.md) | PR-6 API v2 Layer (FastAPI + SSE) | Done | `api/v2/{__init__,schemas,deps,errors,sse,auth,tasks,copilot,health,router}.py`, `config.py` (+4 cookie/SSE fields), `tests/api/*` | `pytest -q` + `ruff check api/v2 tests/api` + `mypy api/v2` | **373 passed**（+36）/ ruff 0 / mypy 0（api/v2 内） | 13 个 `/api/v2/*` 端点（auth × 5 / tasks × 4 / copilot × 2 / health × 2）+ SSE 流式（event/keepalive/error 三类帧 + `asyncio.wait_for` 超时心跳）+ DomainError MRO 状态码映射表 + closure 路由构建器（`build_*_routes(container)`）+ cookie 会话（httponly+samesite=lax）+ 全 Fake 注入测试 fixture；严格 Strangler Fig：未触碰老 `service.py`/`api/routes.py`/`main.py` | Step 011 接入 main.py + 前端改造 / Step 012 PR-7 risk 路由 |
| [011](step_011_pr6_main_integration.md) | PR-6 收尾：api/v2 接入 main.py | Done | `main.py` (+18/-8), `api/v2/errors.py` (path_prefix 守门员), `tests/api/test_main_integration.py` | `pytest -q` + `ruff check main.py api/v2 tests/api/test_main_integration.py` + `mypy main.py api/v2` | **380 passed**（+7）/ ruff 0 / mypy 0（新代码内） | `uvicorn main:app` 启动即可访问 `/api/v2/*`（11 个端点全可达）+ 老 `/api/*` 0 回归（`install_exception_handlers` 按 `path_prefix="/api/v2"` 守门，老路由继续走 FastAPI 默认 `{"detail":...}` 契约）+ CORS `allow_credentials=True`（cookie session 前置条件）+ `app.state.container` 单例 + 集成测试覆盖 v1/v2 共存 | Step 012 前端 Copilot UI 改造 / Step 014 PR-7 risk 子路由 |
| [012](step_012_pr7_frontend_copilot_ui.md) | 前端 Copilot UI 改造（vanilla JS + SSE） | Done | `frontend/{index.html,style.css,api.js,sse.js,auth.js,chat.js,tasks.js,app.js}` 全新写；老 3 件套 git mv → `*.legacy.*` | `pytest -q` + 静态资源 TestClient 烟雾 + 浏览器手测 | **380 passed**（无回归）/ 9 个静态资源 200 / 浏览器自动 me→anonymous→ready→tasks 全 OK | 单聊天对话界面 + sidebar 任务列表 + SSE 流（fetch+ReadableStream，60 行帧解析）+ 9 类 agent 事件 → DOM（thought/tool_call/tool_result/answer/ask_user/citations/task_created/error/keepalive）+ 自动匿名兜底 + GitHub 登录跳转 + 800px 响应式 + 零构建依赖（ES module）；删 109KB 老 UI 代码（保留 .legacy.* 参考） | Step 013 admin baseline + Task.mode 三业务模式 + RiskProfilePort 接口预留 |
| [013](step_013_admin_modes_risk_profile_port.md) | admin baseline + Task.mode 三业务模式 + RiskProfilePort 接口预留 | Done | `domain/{models,ports,errors}.py`, `infra/risk_profile/{__init__,stub_risk_profile}.py`, `infra/storage/{_db,sqlite_task_repo}.py`, `app/{factories,container}.py`, `app/use_cases/{task_management,run_copilot}.py`, `api/v2/{deps,schemas,auth,copilot,tasks}.py`, `config.py`, `.env.example`, `tests/{api,app,domain,infra}/*` | `pytest -q` + `ruff check .` + `mypy app domain infra/risk_profile api/v2` | **409 passed**（+29）/ ruff 0 / mypy 0 | 三件事同 commit：(1) admin user_id 白名单 + `make_require_admin` 401/403 + `UserOut.is_admin` 输出层；(2) `Task.mode = "qa"\|"research"\|"profile"` 全链透传（domain/infra/app/api）+ SQLite 列幂等迁移；(3) `EvidenceState`/`EvidenceSpan`/`RiskProfile` domain + `RiskProfilePort.assess` Protocol + `StubRiskProfileService(raise/placeholder)` + `build_risk_profile` 工厂；与 `schema-evidence-risk-profiling` 仓 v1 schema 对齐，模型部署只需替换工厂实现 | Step 014 前端 3-Tab UI |
| [014](step_014_frontend_3mode_tabs.md) | Frontend 三业务模式 Tab UI | Done | `frontend/{index.html,style.css,chat.js,app.js,tasks.js,auth.js}` | `pytest -q` + 浏览器手测 | **409 passed**（前端不影响后端用例）/ 3 Tab × placeholder/welcome/建议问差异化 / 任务列表徽标研究琥珀 / 画像紫色 | 顶部 ARIA tablist 3 Tab（💬/🔬/📊）+ `_currentMode` 模块状态 + `MODE_PRESETS` 三模式 UI 预设 + `applyMode` 统一同步（高亮/badge/placeholder/welcome）+ 切 Tab 自动 `newConversation()`（三业务不共享上下文）+ `loadTask` 反向同步 mode + sendMessage 仅在创建新 task 时塞 mode + profile Tab 标"接口预留 · evidence-state 模型训练中" | Step 015 profile mode 联调闭环 |
| [015](step_015_profile_mode_wiring.md) | profile 模式联调闭环（RunCopilot 短路 + RiskProfilePort 装配） | Done | `app/use_cases/run_copilot.py`, `app/container.py`, `tests/app/test_run_copilot.py` | `pytest -q` + 浏览器手测 | **413 passed**（+4）/ ruff 0 / mypy 0 | `RunCopilotUseCase.__init__` 加 `risk_profile: RiskProfilePort \| None`；`stream` 在 task_created 后判 `mode=="profile"` 短路 agent；`_run_profile` 三态兜底（None / `RiskProfileNotReady` / 成功）+ `_format_risk_profile_md` 把 `RiskProfile` 渲染为 emoji+label+解释+证据列表 markdown；container 自动透传；qa/research 零回归（专门用例 `_ExplodingRiskProfile` 验证 qa 不调 risk_profile） | Step 016a KB 管理重构 infra 层 |
| [016a](step_016a_kb_document_repo_port.md) | KB 管理重构 第 1 阶段（KbDocumentRepoPort + ChromaKbRepo + Fake + DI） | Done | `domain/{models,ports,__init__}.py`, `infra/kb/{__init__,chroma_kb_repo}.py`, `tests/{fakes/fake_kb_repo,infra/test_chroma_kb_repo,infra/test_fakes,domain/test_models,app/test_container}.py`, `app/{factories,container}.py` | `pytest -q` + scoped `ruff check` + `mypy domain infra/kb app/factories.py app/container.py` | **440 passed**（+27）/ ruff 0 / mypy 0（新代码） | 完整 DDD 重构方案 B 第 1 阶段：`KbDocument`/`KbChunk`/`KbSourceType` 三 frozen model + `KbDocumentRepoPort` 5 方法 Protocol（"先删后插"幂等契约写进 docstring）+ `ChromaKbRepo` 适配老 `VectorStore`（兜底 `"unknown"→"file"`、空 url→None、set 去重 source 后 delete）+ `FakeKbRepo` in-memory 同语义 + `build_kb_repo` 工厂 + `AppContainer.kb_repo` 装配；不动 API/use case/前端/v1 代码，0 回归 | Step 016b KbManagementUseCase（接通 kb_repo） |
| [016b](step_016b_kb_management_use_case.md) | KB 管理重构 第 2 阶段（KbManagementUseCase + DocumentLoaderPort + UnifiedLoaderAdapter + Fake + DI） | Done | `domain/{ports,__init__}.py`, `infra/kb/{__init__,unified_loader_adapter}.py`, `app/use_cases/{__init__,kb_management}.py`, `app/{factories,container}.py`, `tests/{fakes/{__init__,fake_document_loader},infra/{test_fakes,test_unified_loader_adapter},app/{test_container,test_kb_management}}.py` | `pytest -q` + scoped `ruff check` + `mypy app/use_cases/kb_management.py infra/kb` | **465 passed**（+25）/ ruff 0 / mypy 0（新代码） | `DocumentLoaderPort` 2 方法 Protocol（"加载+切分一体、不做 embed/写库"）+ `UnifiedLoaderAdapter` 包 v1 `UnifiedLoader + build_chunks` 做 `RawDocument → list[KbChunk]` 转换 + `FakeDocumentLoader` 三模式（默认/empty/preset）+ `KbManagementUseCase` 编排 3 端口（kb_repo + loader + embedder）暴露 6 方法（4 读 + 2 写）+ `@dataclass(frozen=True) KbIngestResult`+ `build_document_loader` 工厂 + `AppContainer.{document_loader,kb_management}` 装配（10 Port / 5 use case）；不动 API/前端/v1 代码，0 回归 | Step 016c api/v2/documents 路由 |
| [016c](step_016c_api_v2_documents.md) | KB 管理重构 第 3 阶段（api/v2/documents 路由接通 KbManagementUseCase） | Done | `api/v2/{documents,router,schemas}.py`, `tests/api/{conftest,test_documents}.py` | `pytest -q` + scoped `ruff check` + `mypy api/v2/documents.py api/v2/router.py api/v2/schemas.py` | **490 passed**（+25）/ ruff 0 / mypy 0（新代码） | 6 端点全部 admin-only：`GET /documents` / `GET /documents/stats` / `GET /documents/{src}` / `DELETE /documents/{src}` / `POST /documents/file`（multipart + 后缀白名单 + UUID 重命名 + finally 清理 + `anyio.to_thread`） / `POST /documents/web`（urlparse http/https + 域名校验）+ `WebIngestRequest`/`KbDocumentOut`/`KbIngestResponse` 等 6 个 schema + `build_documents_routes` 闭包装入 `build_v2_router` + conftest 注入 `FakeKbRepo`/`FakeDocumentLoader`；25 测试（6 守门 + 19 业务）；v2 前端 KB UI 留独立 PR；v1 路由仍在 | Step 016d v1 删除 + 整体 push |
| [016d](step_016d_v1_kb_removal.md) | KB 管理重构 第 4 阶段（v1 单体 KB 入口删除：收官） | Done | `service.py`, `api/routes.py`, `api/schemas.py`, `domain/ports.py`, `tests/{test_api,test_schemas}.py`, `frontend/*.legacy.*`（git rm） | `pytest -q` + scoped `ruff check` + `mypy service.py api/routes.py api/schemas.py` | **479 passed**（-11，与计划一致：4 + 5 + 2）/ ruff F401 清零 / mypy 46 错与基线持平 | 纯删除：`service.py` -5 KB 方法 / -`IngestResult` / -`loader` 字段 / -`UnifiedLoader` import；`api/routes.py` -4 端点（`/api/ingest/file` `/api/ingest/web` `/api/sources` `DELETE /api/sources/{src}`）+ -`ALLOWED_EXTENSIONS` / `_safe_save_path`；`api/schemas.py` -5 KB schema；`tests/{test_api,test_schemas}.py` 删 3 个测试类；`domain/ports.py` 更新 v1 平行实现的过时注释；3 件归档前端 `*.legacy.*` `git rm`。**不**保留 410 Gone 迁移端点（无真实客户端）；**保留** `KnowledgeService.retrieve/ask/research`（属检索面，不在本次范围）。Step 016 整体收官：KB 管理面 v1 单体 → DDD（10 Port / 5 use case / 6 admin-only 端点）四步落地，净 +66 测试 | 整体 push 016c+016d 到 origin/main |
| [017](step_017_frontend_v2_kb_panel.md) | v2 前端知识库管理面板（admin-only，commit 438deae） | Done | `frontend/{kb.js（新）,api.js,index.html,app.js,style.css}` | `pytest -q` + 浏览器手测 + 静态资源 TestClient | **479 passed**（前端不影响后端用例）/ 5 静态资源 200 / KB 视图 mount 时拉 list+stats 渲染 | 收官 Step 016c 的"v2 前端 KB UI 留独立 PR"承诺：新增 `kb.js`（~165 行 mount/refresh/upload/web/delete + 5 态 status bar）+ `api.js documents` 命名空间 6 方法（list/stats/get/remove/ingestWeb/ingestFile）+ `index.html` 侧栏 chat/kb 双导航 + `app.js _currentView` + `switchView`（同 grid-column 覆盖 + `.hidden` 切换，免路由库）+ `style.css` +231 行 KB 面板样式；当时仍 admin-only（同时藏入口和操作区），permission split 留给 Step 019 | Step 018 登录 bug 修复 |
| [018](step_018_login_bugfix_port_admin.md) | 登录三连击 bugfix：端口对齐 + admin_user_ids 解析 + 启动期守门（commit 53030ad） | Done | `main.py`, `config.py`, `.env.example`, `.env`（local） | `pytest -q` + TestClient admin smoke `_smoke_admin3.py` | **479 passed**（零回归）/ admin 登录 `/auth/me is_admin=True` / `/documents/stats` 200 | 三根因一次清：(1) `main.py` 端口 `8004→8765` 对齐 `settings.github_redirect_uri` 默认 `:8765` + 新增 `_warn_oauth_redirect_port_mismatch(server_port)` 启动期 WARNING 守门；(2) `config.py admin_user_ids` 改 `Annotated[List[str], NoDecode]` + `@field_validator(mode="before")` 同时兼容**逗号分隔**（与历史 docs 一致）和 JSON 数组两种 .env 写法，避免 pydantic-settings v2 默认 JSON-only 死路；(3) `.env` 补 `ADMIN_USER_IDS=github:Melodymll01`（用户名取自 git remote origin URL）。3 文件 +63/-7 | Step 019 KB 读写权限拆分 |
| [019](step_019_kb_permission_split.md) | KB 权限拆分：读端点 login-only / 写端点 admin-only（commit 4ad14ad） | Done | `api/v2/documents.py`, `frontend/{index.html,app.js,kb.js,style.css}`, `tests/api/test_documents.py` | `pytest -q` + 浏览器三角色手测 | **483 passed**（+4 净增：list/stats non-admin allowed + delete/ingest non-admin forbidden）/ 6 文件 +119/-32 | 收用户"其他登录用户应该能看到 KB 内容、不能改"反馈：(1) `documents.py` 3 个 GET 端点 dep `require_admin → require_owner`（写端点保持 admin-only）；(2) 前端"双层 gate" `applyAdminGate → applyKbGate(isLoggedIn, isAdmin)`：分别控制 `#nav-kb`（登录可见） / `#nav-kb-tag` 金标签 / `#kb-actions` 上传区 / `#kb-readonly-banner` 黄色只读提示 banner 的显隐；(3) `kb.js renderList` 读 `getUser()?.is_admin` 渲染删除按钮 vs `—` 占位、空 KB 文案按角色分；(4) `TestAuthGating` 整类改写（原 `non_admin_forbidden → non_admin_allowed`，加 delete/ingest non-admin 403）；权限矩阵：未登录全 401 / 登录可读不可写 / admin 全开 | Step 020 profile mode 真实评测模型联调 或 私人 KB owner_id 隔离 |
| [020](step_020_github_actions_ci_revival.md) | 复活 GitHub Actions CI（scoped ruff + 全量 pytest · commit 86ca837） | Done | `.github/workflows/ci.yml`, `README.md`（+4 徽章）, `config.py`/`api/v2/auth.py`/`tests/api/test_auth.py`（scoped ruff --fix 清扫） | `pytest -q --ignore=eval_ood --ignore=smoke_bm25_rrf` + scoped `ruff check` + GitHub Actions 首跑 | **483 passed**（零回归）/ scoped ruff 0 errors / 首次 push 触发 CI 在 GitHub 跑 | 把 Step 002 起就冻在 `workflow_dispatch` 的 ci.yml 复活：触发条件改 `push:main + PR:main + workflow_dispatch`；删 mypy job（D3：~46 错与基线持平留独立 Step）+ 删 matrix（D4：单 Python 3.12 与 .venv 对齐）+ 删 ruff format check（D5：33 文件待 reformat 留独立 commit）+ 删 coverage upload；2 job 并行（lint scoped 14 路径 / test 8 个假凭据环境变量）；scoped `ruff --fix` 清 13 处 Step 018 引入的 typing.List/Optional/Annotated 小遗漏；README 顶部 4 徽章（CI / tests 483 passed / Python 3.12 / ruff scoped-clean） | 看 CI 跑稳后选 Step 021：结构化日志 + admin 审计 或 私人 KB owner_id 隔离 |
| [021](step_021_admin_audit_log.md) | admin 操作审计日志（AuditLogPort + SqliteAuditLogRepo + KbManagement hook + `/api/v2/audit/logs`） | Done | `domain/{models,ports,__init__}.py`, `infra/audit/{__init__,sqlite_audit_repo}.py`（新）, `app/{factories,container,use_cases/kb_management}.py`, `api/v2/{schemas,audit（新）,router,documents}.py`, `tests/{domain,infra,app,api}/*`（+36 用例）, `.github/workflows/ci.yml`（scoped ruff +`infra/audit`） | `pytest -q --ignore=eval_ood --ignore=smoke_bm25_rrf` + scoped ruff | **519 passed**（+36 净增）/ scoped ruff 0 errors | DDD 全套：(1) `AuditEntry` frozen + `AuditAction` 常量 + `AuditLogPort` Protocol（runtime_checkable，5 字段 + extra_json 任意 dict + request_id 字段已留）；(2) `SqliteAuditLogRepo` 幂等迁移（`audit_log` 表 + `idx_audit_actor`/`idx_audit_action` 双索引），按 timestamp DESC + id DESC 排序；(3) `KbManagementUseCase` 写 3 操作（delete/ingest_file/ingest_web）成功+失败双路径都落审计；audit 写失败仅 logger.warning 不影响主业务；`audit_log=None` 静默跳过保向后兼容；(4) `/api/v2/audit/logs?limit=&action=&actor_id=` admin-only 只读端点（限 limit∈[1,500]）；(5) 决策：审计是副作用而非主流程（D1）/ extra_json 保留写自由（D2）/ request_id 字段先留位（D3）/ 删除不存在资源 use case 视角=成功幂等，404 由 API 层判（D4）/ admin-only 读取，匿名/普通用户即使是自己产生的审计也看不到（D5） | Step 022 待选：审计 UI / 私人 KB owner_id 隔离 / mypy 复活 |
| [022](step_022_architecture_doc_refresh.md) | 架构文档刷新（overview 重写 + ADR-009..013 增量 + 老 ADR Augmented-by 标记） | Done | `docs/architecture/overview.md`（重写）, `docs/decisions/ADR-{009..013}.md`（新 5 个）, `docs/decisions/ADR-{001,006,007,008}.md`（顶部加 Augmented-by 注脚） | `pytest -q --ignore=eval_ood --ignore=smoke_bm25_rrf` + scoped ruff（仅校验代码零回归） | **519 passed**（与 Step 021 一致零回归）/ scoped ruff 0 errors | 纯文档 Step：(1) `overview.md` 完全重写：演进时间轴 / 13 Port 分类表 / 6 use case 表 / 3 个 Mermaid 时序图（OAuth/Copilot ReAct/审计副作用）/ 完整 KB 权限矩阵 / 测试金字塔 / CI 现状 / 13 ADR 全量索引；(2) 5 个新 ADR 追溯整理 Step 008-021 关键决策：ADR-009 Closure Router + Container DI / ADR-010 Strangler Fig v1/v2 共存 / ADR-011 ReAct 自实现 + LLM JSON 协议 / ADR-012 admin RBAC 白名单 + 401/403 / ADR-013 审计副作用语义；(3) ADR 演化原则：只追加不改写，老 ADR-006/001/007/008 顶部加 Augmented-by 注脚但主体字节对字节未变；(4) D4 ADR 准入标准定义（跨多 Step + 涉及外部契约 + 有否决备选 + 影响后续设计）；(5) 不做：不重写 experiment_v1.md / interview_doc / README（留 Step 024）| Step 023 admin 审计 UI |
| [023](step_023_admin_audit_ui.md) | admin 审计日志前端面板（offset 分页 + admin 侧栏入口 + action/actor 过滤） | Done | `domain/ports.py`, `infra/audit/sqlite_audit_repo.py`, `api/v2/audit.py`, `tests/fakes/fake_audit_log.py`, `tests/{infra,api}/test_*audit*.py`（+8 用例）, `frontend/{api.js,index.html,admin-audit.js（新）,app.js,style.css}` | `pytest -q --ignore=eval_ood --ignore=smoke_bm25_rrf` + scoped ruff + 浏览器手测 | **527 passed**（+8 净增 offset 用例）/ scoped ruff 0 errors | (1) 后端 `AuditLogPort.list_recent` + SqliteAuditLogRepo + FakeAuditLogRepo 同步加 `offset: int = 0` kw-only；SQL 加 `LIMIT ? OFFSET ?`；fake 用 list slicing 同语义；(2) `/api/v2/audit/logs` 新增 `offset: int = Query(0, ge=0)` 参数，老 client 不传仍 offset=0 向后兼容；(3) 前端 `api.js` 加 `audit.list({limit,offset,action,actor_id})`；`index.html` 侧栏新增 `nav-audit` admin-only 入口 + `audit-pane` 主区（toolbar/table/pager 三段）；新文件 `admin-audit.js` ~180 行 mount/refresh/分页/action 下拉/actor debounce/extra_json 摘要 + 完整 escapeHtml；`app.js _currentView` 扩 `chat\|kb\|audit` 三态 + `applyKbGate` 内增 audit 入口 gating；`style.css` +180 行 `.audit-*` 复用 KB 色板；(4) 决策：D1 offset 而非 cursor（admin 翻看场景足够，复杂度低）/ D2 不返回 total（额外 COUNT 性能负担，前端按 count<limit 判到底足够）/ D3 不引入 ListAuditLogsUseCase（与 Step 021 风格一致：纯只读 API 直调 port，不强行加贫血 use case）/ D4 不导出 CSV / 不加时间范围过滤（留候选 Step） | Step 024 文档对齐（interview_doc / README）或 Step 025 owner_id 隔离 |
| [024](step_024_docs_alignment.md) | 项目门面文档对齐（README v2 化 + interview_doc 增量补档，本地） | Done | `README.md`（5 大段重写：headers/badges/演进时间轴/Mermaid 架构 + 功能矩阵 + DDD 项目结构 + v1+v2 API 表 + 项目文档段），`interview_doc/*`（gitignored，本地新增补档 + 精修 STAR 文档） | 浏览器预览 Mermaid 渲染 + `git status` 校验 interview_doc 不入 git + pytest/ruff（零代码回归） | **527 passed**（零回归）/ scoped ruff 0 errors / `git ls-files interview_doc/` = 0 | 受众分流：Step 022 文档面向开发者，本步面向 GitHub 访客 + 面试官 + 自己。(1) README 374 行，2 张 Mermaid（4 层 DDD + Agentic 环路）+ 3 角色 × 8 能力功能矩阵 + 17 个 v2 端点表 + 5 个 v1 已删端点删除线标注 + `.env` 示例追加 OAuth/JWT/ADMIN_USER_IDS；(2) interview_doc/ 走「补档 + 精修」非全量重写：新增 `2026-补充-DDD重构与v2API.md`（6 节覆盖 DDD/Strangler/OAuth/RBAC/Audit/Closure Router/ReAct 自实现/测试涨幅/CI/ADR 10 个新 Q&A + v1→v2 话术映射表 + GitHub 必看链接清单），精修 `项目经历-面试用.md` 简历版 + Q7 不足项 + 末尾追加 Q11-Q20 索引；4 篇 unchanged；(3) 决策：D1 补档+精修而非全量重写（保护已熟记话术）/ D2 interview_doc 继续 git-ignore（个人面试策略不公开）/ D3 README 演进段用 ASCII 而非 Mermaid（差异化 + 兼容性）/ D4 已删端点用删除线列出（帮老 fork 用户找新端点）/ D5 README 文档段直接列 9 个 ADR 链接（不折叠，面试官扫读友好） | Step 025a owner_id 隔离 / 025b mypy 复活 / 025c 登录审计 / 026a audit CSV |
| [025a](step_025a_owner_id_tenancy.md) | 私人知识库 owner_id 多租户隔离落地（KB 域 ADR-008 实装：14 后端 + 5 前端 + 5 测试文件，commit 待 push） | Done | `domain/{models,ports}.py`, `processing/metadata.py`, `retrieval/search/{vector_store,bm25_index,retriever}.py`, `infra/kb/{chroma_kb_repo,unified_loader_adapter}.py`, `infra/search/hybrid_retriever.py`, `app/{container,use_cases/kb_management}.py`, `api/v2/{schemas,documents}.py`, `main.py`, `tests/{api/test_documents,app/test_kb_management,infra/test_{chroma_kb_repo,hybrid_retriever,vector_store_owner（新）},fakes/{fake_kb_repo,fake_document_loader}}.py` | `pytest -q` + scoped ruff 21 路径 | **555 passed**（+28：11 TestOwnerScope + 17 test_vector_store_owner）/ scoped ruff 0 errors | KB 多租户全套：(1) `Chunk`/`KbChunk` 加 `owner_id: str \| None`，`None`=公共；物化到 Chroma 时通过 `PUBLIC_OWNER_MARKER = "__public__"` 字符串映射（Chroma 不支持 `{owner_id: None}` 过滤）；(2) `vector_store` `_build_owner_clause` 把 owners 集合翻译成 `{owner_id: v}` 或 `{owner_id: {$in: […]}}` 或必不命中（空集合防误返）；`delete_by_source` 加 `_Unset` sentinel 区分「未传=admin 全能删」和「显式 None=仅删公共」；`add_chunks` 按 `(source_name, owner_id)` 维度先删后插，普通用户上传不会误抹公共同名文档；新增 `migrate_owner_id_marker()` 启动幂等迁移；(3) `KbManagementUseCase` 加 `Scope = Literal["public","mine","all"]` + `_resolve_owners(viewer_id, viewer_is_admin, scope)` helper：admin scope=all → None（不过滤）/ 普通用户 scope=all → `[None, viewer_id]`（公共 ∪ 自己）/ scope=mine → `[viewer_id]` / scope=public → `[None]`；写端点解锁：普通用户 POST 强绑 `owner_id=actor_id`，DELETE 仅删自己；(4) `api/v2/documents.py` 3 GET 加 `?scope=`，POST 加 admin-only `?as_public=true`（admin 默认 False=入自己私人，UI 层 admin 默认勾选 as_public=true）；DELETE 删 0 一律返 404 不暴露存在性；(5) BM25 全局索引不拆，靠 `_owner_matches` 后置过滤 + 候选 `top_k*4` 兜底；(6) 启动迁移在 lifespan `await asyncio.to_thread(container.startup_migrations)`，失败仅 warning；(7) 前端 KB 面板加 scope toggle 三态 + owner 徽章（公共绿/我的蓝/他人灰）+ admin 上传 as_public 复选框 + 行级删除按 owner 判定显隐；(8) 决策：D1 domain None/物化 marker 双层 / D2 单 collection + metadata 过滤（拒 per-user collection）/ D3 暴露 scope 而非 owner_id（业务概念 vs 数据维度）/ D4 普通用户解锁写但 owner 强绑、admin 保守默认入私人 / D5 启动懒迁移 hasattr 守门 + warning 不阻塞；ADR-008 KB 域落地完成（顶部注脚指向本步） | Step 025b mypy 复活 / 025c 登录审计 / 027 文件级分享 |
| [025c](step_025c_auth_audit.md) | 登录端点接入 AuditLogPort（审计闭环） | Done | `domain/models.py`（AuditAction +3 常量）, `app/use_cases/auth_login.py`（注入 audit_log + 3 hook + `_record_audit` helper + request_id 形参留位）, `app/container.py`（AuthLoginUseCase 注入 audit_log）, `tests/app/test_auth_login.py`（+6 TestAuditHooks 用例）, `tests/api/test_audit.py`（admin_client fixture 登录后清 entries） | `pytest -q` + scoped ruff 5 路径 | **560 passed**（净增 +5：6 audit hook 用例 - 1 fixture 调整无新用例只清污染；`test_jwt_issuer::test_tampered_token_returns_none` flaky 单跑通过与本步无关）/ scoped ruff 0 errors | Step 021 审计基础设施收尾：(1) `AuditAction` 追加 `AUTH_LOGIN_SUCCESS` / `AUTH_LOGIN_FAILURE` / `AUTH_ANONYMOUS_CREATE` 三常量；(2) `AuthLoginUseCase.complete` 用 try/except 包 `complete_oauth`：成功 → `AUTH_LOGIN_SUCCESS` + actor=user.user_id + resource=`oauth:{provider}` + extra={"provider"}；OAuthFlowError → `AUTH_LOGIN_FAILURE` + actor=`system:unknown` + error=str(exc) + extra.reason=type(exc).__name__ + re-raise；`login_anonymous` 成功 → `AUTH_ANONYMOUS_CREATE` + resource=`anonymous`；`begin` / `identify` / `require` 不落审计；(3) `_record_audit` 私有 helper 与 `KbManagementUseCase` 同名方法语义一致（audit=None 跳过 / 写失败仅 logger.warning）；(4) `complete` / `login_anonymous` 加 kw-only `request_id: str \| None = None` 形参位（API 层暂不透传，留待 RequestContext middleware）；(5) `api/v2/auth.py` 零改动（D1：审计责任在 use case 层而非 API 层）；(6) `tests/api/test_audit.py` admin fixture 登录后 `audit_log.entries.clear()` 清污染（解决 admin 登录现在会写一条 auth.login_success 导致 entries=[] 断言失败）；(7) 决策：D1 use case 层挂 hook（与 KbManagement 对齐）/ D2 失败用 `system:unknown` 占位（OAuth 未完成时无可信 user_id，失败本身就是 admin 关心的信号）/ D3 begin 不落审计（无副作用 URL 生成，写等于噪音）/ D4 request_id 形参留位本步不透传（与 Step 021 D3 一致）/ D5 resource=`oauth:{provider}` / `anonymous`（语义是被操作的认证流程，非 user 也非 HTTP path） | Step 025b mypy 复活 / 025d RequestContext middleware / 025e logout 端点 + 审计 / 026a audit CSV |
| [025d](step_025d_request_context_middleware.md) | RequestContext middleware（激活 request_id 透传） | Done | `app/request_context.py`（新：contextvar + helpers + `install_request_id_middleware`）, `app/use_cases/{kb_management,auth_login}.py`（`_record_audit` 形参 fallback contextvar）, `main.py`（middleware 改用 set/reset 原语）, `tests/api/conftest.py`（app fixture 挂 middleware）, `tests/app/test_request_context.py`（新 11 单测）, `tests/api/test_request_id_propagation.py`（新 6 端到端） | `pytest -q` + scoped ruff 7 路径 | **578 passed**（净增 +18：11 单测 + 6 端到端 + 1 偶发被覆盖通过；零失败）/ scoped ruff 0 errors | (1) `request_id_var: ContextVar[str \| None]` 默认 `None`；`set_request_id(value)` 返回 reset token；`reset_request_id(token)` 还原；`request_context(value)` contextmanager 给测试/命令行用；`install_request_id_middleware(app)` 注册 FastAPI middleware（读 `X-Request-ID` header 或 `uuid4().hex[:12]` 自动生成 → set contextvar + `request.state.request_id` → try/finally 包 `call_next` → response 回写 header）；(2) `_record_audit` helper 内 `effective_request_id = request_id if request_id is not None else get_request_id()`，"显式形参优先 > contextvar > None" 三段；KbManagement + AuthLogin 两处对称；(3) main.py 原 inline `request_context_middleware` 保留（合并 logging + elapsed_ms + path 过滤），改用 `set_request_id`/`reset_request_id` 原语 + try/finally；(4) `tests/api/conftest.py` app fixture 调 `install_request_id_middleware`，让端到端测试与生产同形态；(5) 决策：D1 ContextVar 而非 explicit threading（Strangler 原则：use case 不该知道 web 概念）/ D2 三段优先级（形参 wins 保留批量回放/单测能力）/ D3 函数式 `install_*(app)` + main.py 保留 inline（与 `install_exception_handlers` 风格一致；main.py 还要合并 logging 不适合搬走）/ D4 12 字符 hex（48 bit 抗冲突 + 与 main.py 既定一致）/ D5 try/finally 包 call_next（失败请求也回写 header + reset contextvar） | Step 025e logout + 审计 / 025f 结构化日志 contextvar / 026a audit CSV |
| [025e](step_025e_logout_audit.md) | logout 端点接入 AuditLogPort（身份生命周期闭环） | Done | `domain/models.py`（AuditAction.AUTH_LOGOUT 常量）, `app/use_cases/auth_login.py`（新增 `logout(token, request_id=None) -> str \| None` 方法）, `api/v2/auth.py`（logout 路由从 cookie 取 token + 调 use case + 清 cookie）, `tests/app/test_auth_login.py`（+7 TestLogout 用例）, `tests/api/test_auth.py`（+2 端到端：登录态写审计 + 未登录 silent） | `pytest -q` + scoped ruff 5 路径 | **587 passed**（净增 +9：7 单测 + 2 端到端）/ scoped ruff 0 errors | (1) `AuditAction.AUTH_LOGOUT = "auth.logout"`；(2) `AuthLoginUseCase.logout` 调 `self.identify(token)` 解出 user_id：None → 直接返回 None **不**写审计（D2：避免任意客户端 spam logout 灌表）；非 None → 写 actor=user_id + resource=`session` + extra={} 后返回 user_id；request_id 走 Step 025d contextvar 自动透传；(3) API 路由签名加 `Request`，从 `container.settings.cookie_name` 取 token 调 use case 后丢弃返回值，`clear_session_cookie` 不动；响应契约 `{"ok": true}` 不变（D1：cookie 清理留传输层）；(4) 决策：D1 use case 不接 Response（领域无 web 概念）/ D2 未登录 logout silent no-op（避免审计噪音；与 ADR-013 D5「业务连续性 > 审计完备性」一致）/ D3 resource=`session` 形成 oauth:{provider}/anonymous/session trio / D4 返回 user_id（信息含量高于 bool）/ D5 路由用 `Request.cookies.get` 而非 `Depends(identify)`（避免双重解码 + 让 use case 是身份逻辑的唯一持有者） | Step 025f 结构化日志 contextvar / 026a audit CSV / 027 session expiration 审计 / 025b mypy 复活 |
| [025f](step_025f_logging_request_id.md) | 结构化日志 contextvar 注入（每条 log 自动带 `request_id`） | Done | `app/logging_setup.py`（新：`RequestIdLogFilter` + `configure_logging`）, `main.py`（启动调 configure_logging + 删 middleware/global handler 两处手工 `f"[{request_id}]"`）, `tests/app/test_logging_setup.py`（新 8 用例：filter 行为 + 安装幂等 + 端到端 format） | `pytest -q` + scoped ruff 3 路径 | **595 passed**（净增 +8：3 filter + 5 configure_logging）/ scoped ruff 0 errors | Step 025d 让审计带 request_id；本步把 request_id 提升为 logging 一等字段：(1) `RequestIdLogFilter.filter()` 从 `get_request_id()` 取值 → `record.request_id`，缺省 `"-"`（Apache log 哨兵约定）；(2) `configure_logging(*, level, log_file, fmt, extra_handlers)` 替代 `logging.basicConfig`：format 加 `[%(request_id)s]` 段，filter 挂 *handler* 级而非 logger 级（stdlib 文档明示 logger 级 filter 不级联），按 `_step025f_owned` 标记 + 类型双重去重防多次调用 handler 翻倍；(3) main.py middleware logger.info 与 global_exception_handler logger.exception 删手工拼接，formatter 透明加；evaluations/ 离线脚本不动；(4) 决策：D1 Filter 而非 Formatter 子类（零侵入 + stdlib cookbook 推荐）/ D2 缺省 `"-"`（一字符不污染列宽 + nginx access log 一致）/ D3 handler 级 filter（logger 级不级联）/ D4 `_step025f_owned` 标记幂等（不抢 uvicorn/pytest 已有 handler）/ D5 `extra_handlers` 仅测试注入（prod 路径只 stream + 可选 file，不破坏配置面最小性） | Step 025g user_id 注入 contextvar / 026a audit CSV / 025b mypy 复活 |
| [025g](step_025g_user_id_contextvar.md) | `user_id` 注入 contextvar（log 行加 `[uid:xxx]` 段） | Done | `app/request_context.py`（`user_id_var` + `get_user_id`/`set_user_id`/`reset_user_id` + `request_context(rid, *, user_id=None)` 扩签）, `app/logging_setup.py`（filter 同时注入 `record.user_id`；`DEFAULT_FORMAT` 加 `[uid:%(user_id)s]` 段）, `main.py`（middleware 解 cookie → `auth_login.identify` → `set_user_id`/`reset_user_id`）, `tests/app/test_logging_setup.py`（+1 `test_user_id_independent_of_request_id` + 既有用例补 user_id 断言） | `pytest -q` + scoped ruff 4 路径 | **596 passed**（净增 +1：1 新 filter 用例；既有 8 用例 in-place 增强 user_id 断言）/ scoped ruff 0 errors | Step 025f 让 log 带 request_id；本步让 user_id 也走 contextvar：(1) `request_context()` 签名扩为 `(request_id, *, user_id=None)` —— 17 个既有位置参数调用点零修改；(2) `RequestIdLogFilter` 名字保留（D4），filter() 同时注入 `record.request_id` 与 `record.user_id`，format 增 `[uid:%(user_id)s]` 段；(3) main.py middleware 内只对 `/api/` 路径解 cookie token + 调 `auth_login.identify`，try/except 兜底失败 silent → user_id 退到 `None`（出 `[uid:-]`）；access log 行天然带 uid；(4) `install_request_id_middleware` 测试 fixture 不动（不接 container）；(5) 决策：D1 middleware 层解析（一处 set 全链路覆盖 + access log 也带 uid）而非 yield-form Depends（dependency 在 access log 之前没机会 set）/ D2 失败 silent 退 None（绝不让认证逻辑带崩请求）/ D3 kw-only user_id 保位置参数兼容 / D4 filter 名字保留（重命名爆破 17 个 import）/ D5 测试 fixture 不解析 user_id（端到端只验 request_id 透传，user_id 由单测覆盖） | Step 026a audit CSV / 025b mypy 复活 / v1 检索面退役 |
| [026a](step_026a_audit_csv_export.md) | Admin 审计日志 CSV 导出（admin 面板「⬇ CSV」按钮 + 流式 `GET /audit/export.csv`） | Done | `api/v2/audit.py`（`GET /audit/export.csv` + `_stream_csv` / `_flush` / `_entry_to_row` helper + `CSV_HEADER` / `DEFAULT_MAX_ROWS=10000` / `CSV_FLUSH_EVERY=200`）, `tests/api/test_audit.py`（+`TestExportCsv` 7 用例）, `frontend/api.js`（`audit.exportCsvUrl()` 构 URL）, `frontend/admin-audit.js`（`#audit-btn-export` 点击事件 → `<a download>` 触发原生下载）, `frontend/index.html`（toolbar 加 `<button id="audit-btn-export">⬇ CSV</button>`） | `pytest tests/api/test_audit.py -q` + 整轮回归 + scoped ruff | **604 passed**（净增 +7：7 TestExportCsv 用例）/ scoped ruff 0 errors | (1) `GET /api/v2/audit/export.csv?action=&actor_id=&max_rows=10000` admin-only；`StreamingResponse` + `csv.writer` 按 200 行 chunk flush；起头 yield UTF-8 BOM 让 Excel 双击中文不乱码；(2) 复用 `list_recent(limit=max_rows, action=, actor_id=)` 拉取（行数受 `max_rows ≤ 10000` 强校验保护），不扩 `AuditLogPort` 协议；(3) CSV 9 列：`timestamp_iso` (UTC ISO 8601 ms) / `timestamp_epoch` (float) / `actor_id` / `action` / `resource` / `request_id` / `success` (0/1) / `error` / `extra_json` (原 JSON 字符串)；时间双列既 Excel 排序友好又机器友好；(4) `Content-Disposition: attachment; filename="audit_export_{UTC ISO 时间戳}.csv"` + `Cache-Control: no-store` 防代理缓存敏感数据；(5) 前端按钮：`<a download>` 触发浏览器原生下载（避开把 CSV 全读进 JS 内存的 fetch+Blob 路线）；按当前 actionFilter / actorFilter 透传过滤参数；(6) 决策：D1 `StreamingResponse` + `csv.writer` 分块 yield（客户端能看到下载进度，不卡白屏）/ D2 不引入 `since` / `until`（留 Step 026b 候选；4 处改动不属于「最小可用」）/ D3 `max_rows=10000` 硬上限（5MB 负载，Excel/Pandas 秒开；按 action+actor 切片 + 后续 time range 即可分批）/ D4 timestamp 双列（仅 ISO Excel 排序坑，仅 epoch 人类不可读）/ D5 `extra_json` 保留原 JSON 字符串（schema 不固定不展开列；下游 `pandas.json_normalize` 按需）/ D6 UTF-8 BOM（Excel 默认 GBK 解会乱码；测试用 `utf-8-sig` 剥 BOM）/ D7 前端 `<a download>` 不走 fetch（流式 + 0 JS 内存峰值） | Step 026b 时间范围过滤 / Step 026c 审计归档自动化 / 025b mypy 复活 |
| [026b](step_026b_chat_provider_split.md) | Chat 通道独立 API key / base url 配置（embedding 留智谱、chat 切百炼 GLM-5 / 通义 Qwen 拿免费额度） | Done | `config.py`（`chat_api_key: str \| None = None` + `chat_api_base: str \| None = None`；`effective_chat_api_key` / `effective_chat_base_url` 优先链 `chat_* or openai_*`）, `retrieval/generation/chat_client.py`（`__init__` 改用 `effective_chat_*`；删 local/api 分支判断；顺手清 14 处 ruff 旧式 typing 警告）, `tests/test_config_chat_override.py`（新建 11 用例：4 chat key / 4 chat base / 2 embed 不受影响 / 1 ChatClient 实际拿到覆盖值）, `.env`（拆 4 段重写 + UTF-8 修乱码 + 加 `CHAT_API_KEY` / `CHAT_API_BASE` / `CHAT_MODEL=glm-5`） | `pytest -q --ignore=eval_ood --ignore=smoke_bm25_rrf` + scoped ruff 3 路径 | **615 passed**（净增 +11：11 TestChatApi*Override + TestEmbedNotAffected + TestChatClientUsesEffective）/ scoped ruff 0 errors | (1) 单边覆盖只加 `chat_*` 不对称加 `embed_*`（D1 YAGNI：embed 切家必然要重建 ChromaDB，没人主动做）；(2) 优先链 `chat_api_key or openai_api_key`：None / 空串都 falsy 回退，非空覆盖（D2 测试 `test_empty_string_treated_as_falsy_and_falls_back` 锁死）；(3) 顺手修 `chat_client.py` 封装 bug：旧代码绕过 `effective_*` 直接读 `settings.openai_api_key`，导致 `CHAT_API_KEY` 覆盖**无效**（D3 必修）；(4) 测试 autouse fixture `_isolate_env` 用 `monkeypatch.delenv` 清 9 个 env 变量，防前序测试通过 `setenv` / `load_dotenv` 污染 process env 让 pydantic-settings 注入（D4：单跑 11 过、整轮先挂 8 → 修完整轮 615 全过）；(5) `.env` 同时修了历史 GBK→UTF-8 双重编码乱码（编辑器用 PowerShell 5.1 默认 GBK 写中文注释造成的，重写为 UTF-8 无 BOM）；用户场景：embedding 留智谱 `embedding-3` ChromaDB 0 重建，chat 切百炼代理的 GLM-5 拿 1M token 免费额度 | Phase 2 端到端验证（用户填 key 后）/ Step 026c 对称 `EMBED_API_*`（按需）/ Step 026d 审计归档自动化 |
| [026c](step_026c_chat_process_collapse_ui.md) | 前端 chat GPT 式「推理过程」可折叠卡片：思考/工具步骤先实时展开转圈，产出答案时自动折叠成「推理过程 [N 步] ▾」，答案在卡片外渲染 | Done | `frontend/chat.js`（`ensureProcess`/`processBodyOf`/`bumpProcessCount`/`finalizeProcess`；`renderThought`/`renderToolCall`/`renderToolResult` 改投递到 `.process-body`；`renderAnswer` 先 `finalizeProcess` 再把答案+引用渲染到 process 外；`onError`/`onDone` 兜底 finalize；末尾 `window.__chatDevHooks__` 测试钩子）, `frontend/style.css`（`.process` 家族：summary/spinner→done 绿点/chevron 旋转/body） | Playwright 合成事件注入 4 段断言 | 折叠/展开/计数/答案外置/引用链接全绿；截图佐证 collapsed+expanded 两态 | (1) 原生 `<details>` 而非自造 JS 折叠（D1 无障碍 + 0 依赖）/ (2) lazy create process 容器（D2 首个 thought 才建）/ (3) 答案渲染在 process **外**（D3 折叠后答案仍可见）/ (4) error/done 双兜底 finalize 防永久转圈（D4）/ (5) dev hook 暴露内部函数供 Playwright 注入合成事件（D5）/ (6) 步数 = `.trace`+`.tool-card` 计数（D6） | Step 026d Phase 2 真服务端到端验证 |
| [026d](step_026d_live_e2e_and_json_repair.md) | RAG 链路真服务端到端验证（live e2e：真智谱 embedding-3 + 真百炼 GLM-5）+ 顺带修复 live 暴露的 GLM-5 决策 JSON 未转义引号解析缺陷 | Done | `pytest.ini`（+`live` marker）, `tests/live/`（`conftest.py` 真 `main.app` + `_guard_live` 双重门禁；`test_rag_pipeline.py` 登录→上传《个人信息保护法》→同步问答→断言）, `app/agent/decision.py`（`parse_decision` 加 `_repair_unescaped_quotes` 兜底状态机）, `tests/app/agent/test_decision.py`（+`TestParseDecisionUnescapedQuotes` 4 用例） | `pytest -m live`（RUN_LIVE=1）/ `pytest`（默认）/ `ruff check` | **live 1 passed (67s)**（修复前 1 failed：答案=兜底）/ 默认 **623 passed, 1 skipped** / ruff All checks passed | (1) 新 marker `live` 不复用 `e2e`（语义相反，真服务真花钱真 flaky，D1）/ (2) TestClient 进程内打真适配器而非子进程 uvicorn（D2）/ (3) 自带上传到临时 chroma 零污染真实库（root conftest 把 CHROMA_PERSIST_DIR 重定向到 tmp，D3）/ (4) 双重门禁 RUN_LIVE=1+真 key 默认关闭保护全量轮次不花钱（D4）/ (5) 断言宽松但有意义（非空+领域关键词+有 thought，避 flaky，D5）/ (6) 修解析器而非放宽测试（D6）/ (7) 兜底仅 json.loads 失败后触发、合法 JSON 零改动（D7）；live 第一次跑即暴露真缺陷：GLM-5 在 thought 内塞未转义 ASCII 双引号 `第三章"..."的条款` → `json.loads` line2col38 报错 → Agent 回退兜底答复 | GLM-5 改 function-calling 根治 JSON（大改 ChatPort）/ research 模式 live 覆盖 / `",` 误判边界 |
| [026e](step_026e_decision_json_object.md) | GLM-5 决策 JSON 根治：决策轮启用 `response_format={"type":"json_object"}` 在模型层强制合法 JSON（轻量结构化输出，非完整 function-calling），把 026d 引号修复降级为纵深防御 | Done | `domain/ports.py`（`ChatPort.chat` +`json_mode` 关键字参数）, `retrieval/generation/chat_client.py`（`complete`/`_openai_complete` +`response_format`；`BadRequestError` 去参降级重试一次）, `infra/chat/openai_chat.py`（adapter `json_mode`→`{"type":"json_object"}` 透传 + `_ChatClientLike` 协议同步）, `app/agent/copilot.py`（决策轮 `json_mode=True`）, `tests/fakes/fake_chat.py`（+记录 `json_mode`）, `tests/retrieval/test_chat_client_response_format.py`（新建 4 单测） | `pytest`（默认）/ `pytest -m live`（RUN_LIVE=1）/ `ruff check` | 默认 **627 passed, 1 skipped**（较 026d +4 新单测）/ **live 1 passed (66s)**（GLM-5 原生接受 json_object，未触发降级）/ ruff All checks passed | (1) 轻量 response_format 而非完整 function-calling（保 JSON 协议架构零结构改动，D1）/ (2) `BadRequestError` 优雅降级去参重试一次（换网关不挂，D2）/ (3) `json_mode` 默认 False 向后兼容，仅决策轮显式 True、run_query 保持自由文本（D3）/ (4) 保留 026d 引号兜底作纵深防御（D4）/ (5) 系统提示已含 "json" 满足 OpenAI json_object 约束（D5） | 完整原生 function-calling/tool-calling 迁移（重型，后续）/ research 模式 live 覆盖 |
| [027](step_027_v2_cross_encoder_rerank.md) | v1 检索武器迁移到 v2（一）：CrossEncoder 精排接入——v2 之前 reranker 退化成弱距离阈值过滤（没遵守 `ENABLE_RERANKER=true` 配置），本步提取 `build_reranker()` 工厂接进 v2，懒加载防启动阻塞 | Done | `retrieval/search/reranker.py`（+`build_reranker()` 工厂：enable→CrossEncoder/失败回退距离阈值）, `infra/search/hybrid_retriever.py`（`HybridRetrieverAdapter` 改懒加载 `_ensure_retriever()` 首次 retrieve 才 new Retriever + 注入 `build_reranker()`）, `tests/retrieval/test_reranker_factory.py`（新建 4 单测）, `tests/infra/test_hybrid_retriever.py`（+`TestLazyRetriever` 2 用例） | `pytest`（默认）/ `pytest -m live`（RUN_LIVE=1）/ `ruff check` | 默认 **634 passed, 1 skipped**（较 026e +7）/ **live 1 passed (87s)**（日志佐证 Cross-Encoder 重排 17→17 device=cuda 分数 [0.194,0.999]）/ ruff 新增代码 All checks passed | (1) 懒加载而非容器构造即加载（CrossEncoder 同步加载 1GB 模型会阻塞 `from main import app`，D1）/ (2) 提取 `build_reranker()` 工厂 + 加载失败回退（无模型/无网不让检索挂掉，D2）/ (3) 不动 v1 service.py（Step 029 随 v1 删，范围最小化，D3）/ (4) query_rewriter/BM25-RRF 已在 v2 Retriever 默认值，唯一缺口是 CrossEncoder，D4）/ (5) 不扩散修 reranker.py 20 处既有旧式注解债（不在 CI scope + 避免无关 churn，D5） | Step 028 research 能力迁移 / Step 029 v1 退役收官 |
| [028](step_028_v2_research_migration.md) | v1 检索武器迁移到 v2（二）：深度研究（research）能力迁移——v2 之前 `research` 与 `qa` 同走一个 ReAct agent（名存实亡），本步新增 `ResearchPort` 接进 v1 `AgenticRAGAgent`（分类/OOD/证据分级/报告生成），`research` 模式真正产出长篇结构化报告 | Done | `domain/models.py`（+`ResearchStep`/`ResearchReport`）, `domain/ports.py`（+`ResearchPort`）, `infra/research/agentic_research.py`（新建 `AgenticResearchAdapter`：懒加载包 v1 引擎 + 注入 `build_reranker()` + result→report 映射）, `app/factories.py`（+`build_research()`）, `app/container.py`（装配 `research` Port，11→12）, `app/use_cases/run_copilot.py`（+`mode=="research"` 分支→步骤渲染 thought/报告渲染 answer）, `tests/fakes/fake_research.py`（新建）, `tests/app/test_run_copilot.py`（+`TestResearchMode` 3 用例）, `tests/infra/test_agentic_research.py`（新建 5 用例） | `pytest`（默认）/ `RUN_LIVE=1` 经容器 `run_copilot.stream(mode="research")` / `ruff check` | 默认 **642 passed, 1 skipped**（较 027 +8）/ **live ✅** 事件序列 `task_created→9×thought→answer`，报告 3830 字 2 引用 / ruff All checks passed | (1) 镜像 profile 分支不改 ReAct agent（与既有架构一致零侵入，D1）/ (2) 复用 v1 引擎而非重写（Strangler Fig，与 027 复用 reranker 同理，D2）/ (3) 懒加载沿用 027（避免 1GB 模型阻塞 `from main import app`，D3）/ (4) 决策步骤渲染 thought 可视化推理链路（D4）/ (5) 不做持久化对齐 profile（范围最小，D5） | Step 029 v1 退役收官 |
| [029](step_029_v1_retirement.md) | **v1 整体退役收官**：Step 027/028 已迁完 v1 仅剩的检索/research 武器，本步删 v1 HTTP 层三件套（`service.py:KnowledgeService` 单体门面 + `api/routes.py` v1 端点 + `api/schemas.py`），`main:app` 只剩 `api/v2/*`，v1/v2 并存结束 | Done | **删** `service.py`/`api/routes.py`/`api/schemas.py`/`tests/test_api.py`/`tests/test_schemas.py`, `api/__init__.py`（清空 v1 import 只留 docstring）, `main.py`（移除 `limiter`/`RateLimitExceeded` handler/`start_service_init()`/`include_router(router)` + slowapi import）, `evaluations/benchmark/run.py`（+`BenchmarkRagService` 引擎门面替代 `KnowledgeService`）, `tests/api/test_main_integration.py`（删 `test_legacy_api_health_still_served` + 更新 docstring） | `pytest`（默认）/ `python -c "import main; TestClient"`（live import）/ `ruff check` | **627 passed, 1 skipped**（较 028 −15：删 `test_api`+`test_schemas`+1 legacy health 用例）/ live ✅ `main:app` 无 v1 装配即导入，`/api/v2/health`→200，老 `/health`→**404** / ruff All checks passed（新增代码） | (1) 删 v1 HTTP 层但保留 `retrieval/` 引擎模块（v1/v2/benchmark 共享资产，D1）/ (2) benchmark 改用 `BenchmarkRagService` 复用引擎不重写（与 v2 检索同源，D2）/ (3) 移除 `limiter`（v1 专属 grep v2=0），v2 限流列后续（D3）/ (4) 删根 `/health`（前端走 `/api/v2/health`，无 docker healthcheck，D4）/ (5) 保留 `GET /` 静态前端入口（非 v1 路由，D5） | **迁移完成** → 进入模块策略/算法重设计阶段 |
| [030a](step-030a_memory_l1_short_term.md) | **分层记忆 L1 短期注入 + MemoryAssembler 骨架**：Agent 从「每轮无状态」升级为「带本 task 短期上下文」。只做 L1（最近 N 条历史注入 prompt）与装配器骨架，明确不实现 L2/L3/L4 与固化管线 | Done | `config.py`（+`memory_enabled`/`memory_recent_n`/`memory_token_budget`）, `domain/ports.py`（`MemoryPort.recent_messages` 补 `owner_id`）, **新增** `infra/memory/task_memory.py:TaskBackedMemory`（L1 挂 task 消息表 + owner 校验；L2/L3/L4 抛 `NotImplementedError`）, **新增** `app/memory/assembler.py:MemoryAssembler`（读 L1+排版+token 预算+全程降级）, `app/agent/copilot.py`（注入 `memory_assembler`，写当前消息前读历史块）, `app/factories.py`（+`build_memory`）, `app/container.py`（装配 memory+assembler，12→13 Port）, **新增** `tests/fakes/fake_memory.py` + 隔离/注入/降级三类测试 | `pytest`（默认）/ `import main` live / `ruff check` | 新增记忆 **20 passed**；全量 **648 passed / 1 skipped**（较 029 +21）；ruff All checks passed；`import main` OK v2 路由正常 | (1) L1 复用 task 消息表不双写（D1）/ (2) owner_id 进签名，越权返回空不抛错（安全降级 D2）/ (3) 读历史在写当前消息之前→注入块只含先前轮（D3）/ (4) 记忆块作独立 system 消息插主提示词后（D4）/ (5) token 预算字符近似从最旧丢、至少留最近一条（D5）/ (6) 装配器全程降级不拖垮主对话（D6）/ (7) 只实现 L1，L2/L3/L4 留接口（D7） | Step 030b（L2 摘要+TTL）/ 030c（L4 固化管线）/ 030d（L3 画像+主动遗忘+审计） |
| [030b](step-030b_memory_l2_summary_ttl.md) | **分层记忆 L2 滚动摘要 + TTL 逻辑遗忘 + 异步调度**：更早内容压缩成摘要、过期记忆读时失效、摘要作业回复后异步跑不阻塞主回复。明确不实现 L3/L4 与固化管线 | Done | `config.py`（+`memory_summary_enabled`/`memory_summary_threshold`/`memory_l1_ttl_days`/`memory_l2_ttl_days`）, `domain/models.py`（+`TaskSummary`）, `domain/ports.py`（`get_summary`/`maybe_summarize` 补 `owner_id` + 新增 `SummaryStorePort`/`MemoryJobSchedulerPort`）, `infra/storage/_db.py`（+`task_summaries` 表）, **新增** `infra/storage/sqlite_summary_store.py:SqliteSummaryStore`, `infra/memory/task_memory.py`（+L2 增量精炼+TTL 过滤）, **新增** `infra/memory/scheduler.py:ThreadPoolMemoryScheduler`, `app/memory/assembler.py`（汇聚摘要+原文，摘要优先预算）, `app/use_cases/run_copilot.py`（qa `finally` 调度）, `app/factories.py`（+`build_summary_store`/`build_memory_scheduler`）, `app/container.py`（装配 memory_scheduler）, `main.py`（lifespan 优雅 shutdown）, **新增** `tests/fakes/fake_summary_store.py` + 摘要/TTL/调度/SQLite 多类测试 | `pytest`（默认）/ `import main` live / `ruff check` | 全量 **673 passed / 1 skipped**（较 030a +25）；ruff All checks passed；`import main` OK，`memory_scheduler=ThreadPoolMemoryScheduler` | (1) L2 独立 `task_summaries` 表不挂 task 行（D1）/ (2) owner_id 进 L2 签名越权返回空（D2）/ (3) 增量精炼 O(1)/轮不 map-reduce（D3）/ (4) `msg_watermark` 幂等水位重试自愈（D4）/ (5) TTL 读时过滤不物理删（逻辑遗忘 D5）/ (6) 摘要作业线程池异步不阻塞主回复（D6）/ (7) 调度在 `finally` 断连不漏（D7）/ (8) 装配预算摘要优先原文填剩余（D8） | Step 030c（L4 固化管线 fork）/ 030d（L3 画像+主动遗忘+审计） |
| [030c](step-030c_memory_l4_consolidation.md) | **分层记忆 L4 语义事实固化管线（提取-验证-巩固）+ 写入门控 + 冲突遗忘**：Agent 从「只记本 task」升级为「跨会话记住用户长期事实」。回复后 fork 后台固化、规则+LLM 混合门控、去重强化/冲突取代、容量衰减淘汰、召回注入。明确不实现 L3 画像/主动遗忘/审计 | Done | `config.py`（+10 项 L4 配置）, `domain/models.py`（`Fact` 扩固化字段 + 新增 `ConsolidationState`）, `domain/ports.py`（+`FactStorePort`/`ConsolidationStatePort`、`schedule_consolidation`）, `infra/storage/_db.py`（+`consolidation_state` 表）, **新增** `infra/storage/sqlite_consolidation_state.py:SqliteConsolidationStateStore`, **新增** `infra/memory/fact_store.py:ChromaFactStore`（独立 `memory_facts` collection）, **新增** `infra/memory/consolidation.py:ConsolidationWorker`（三段式+冲突遗忘+容量淘汰+watermark 幂等）, `infra/memory/task_memory.py`（+L4 `recall_semantic`）, `infra/memory/scheduler.py`（+`schedule_consolidation`）, `app/memory/assembler.py`（汇聚补 L4 事实块最优先）, `app/agent/copilot.py`（`_memory_block` 传 query）, `app/use_cases/run_copilot.py`（收尾追加固化调度）, `app/factories.py`（+`build_fact_store`/`build_consolidation_state_store`/`build_consolidation_worker`）, `app/container.py`（装配共享 fact_store/worker）, **新增** `tests/fakes/fake_fact_store.py` + 固化/召回/调度/SQLite 多类测试 | `pytest`（默认）/ `import main` live / `ruff check` | 全量 **702 passed / 1 skipped**（较 030b +29）；ruff All checks passed（`sqlite_task_repo.py` SIM118 为既有遗留）；`import main` OK，`fact_store`/`consolidation_worker` 正常装配 | (1) 写入门控规则+LLM 混合三职责正交（D1）/ (2) 冲突遗忘三分支去重强化/冲突取代/新增（D2）/ (3) 接地+显著性双关早退（D3）/ (4) 逻辑遗忘召回时过滤 superseded/过期不物理删（D4）/ (5) 容量衰减分 `salience·e^(-λ·age)` 淘汰（D5）/ (6) L4 独立 Chroma collection 复用向量栈（D6）/ (7) fork 异步复用 `MemoryJobSchedulerPort`（D7）/ (8) 每轮调度但 MIN_BACKLOG=30 门控（D8）/ (9) 渐进置信+watermark 幂等重试不重复（D9） | Step 030d（L3 画像聚合 + 主动遗忘 `forget()` + 审计；task-close 触发点接线） |
| [030d](step-030d_memory_l3_forget.md) | **分层记忆 L3 用户画像（稳定偏好）+ 主动遗忘（被遗忘权）+ 审计**：Agent 从「记住长期事实」升级为「沉淀稳定偏好 + 支持用户一键清空记忆」。L3 画像跨会话注入、`forget()` 双 scope 级联物理删除、删除计数落审计。明确不实现 L4→L3 自动聚合 / Agent 工具写画像（§14.5 推迟） | Done | `config.py`（+`memory_profile_enabled`/`memory_profile_max_facts`）, `domain/models.py`（+`ForgetResult` + `AuditAction.MEMORY_FORGET`/`MEMORY_PROFILE_UPDATE`）, `domain/ports.py`（`MemoryPort.forget` + 4 Store Port 统一 `delete_owner` + 新增 `ProfileStorePort`）, `infra/storage/_db.py`（+`profiles` 表 PK owner_id）, **新增** `infra/storage/sqlite_profile_store.py:SqliteProfileStore`, `infra/storage/sqlite_summary_store.py`/`sqlite_consolidation_state.py`（+`delete_owner`）, `infra/memory/fact_store.py`（+`delete_owner`）, `infra/memory/task_memory.py`（落地 L3 get/update_profile + `forget` 级联）, `app/memory/assembler.py`（汇聚补 L3 画像块 L4>L2>L3>L1）, **新增** `app/use_cases/forget_memory.py:ForgetMemoryUseCase`（编排 + 审计）, `app/factories.py`（+`build_profile_store`）, `app/container.py`（装配 profile_store + forget_memory）, `api/v2/schemas.py`（+Profile/Forget DTO）, **新增** `api/v2/memory.py`（`GET /memory/profile` + `POST /memory/forget`）, `api/v2/router.py`（挂载）, **新增** `tests/fakes/fake_profile_store.py` + 画像/遗忘/审计/API 多类测试 | `pytest`（默认）/ `import main` live / `ruff check` | 全量 **738 passed / 1 skipped**（较 030c +36）；ruff All checks passed；`import main` OK，`profile_store`/`forget_memory` 正常装配，`/api/v2/memory/*` 路由正常挂载 | (1) L3 独立 `profiles` 表按 owner 不挂 task（D1）/ (2) 起步只显式声明 + 系统配置自动聚合推迟（D2）/ (3) forget 双 scope：memory 清派生保留 L1、all 级联删 task（D3）/ (4) forget 物理删除区别 030c 逻辑遗忘（D4）/ (5) 审计只记计数不回存内容（D5）/ (6) 失败也落审计再抛（D6）/ (7) L3 注入优先级最低整块丢弃不挤占 L1（D7）/ (8) `delete_owner` 统一加 4 Store Port（D8）/ (9) API owner 隔离 + memory 禁用降级 200（D9） | 记忆系统收官；后续 L4→L3 自动聚合 / Agent 工具写画像 / task-close 触发接线 |
| [031a](step-031a_memory_settings.md) | **每用户记忆设置（双开关）+ 注入门控 + 管理读接口**：把记忆控制权交还用户。ChatGPT 式两开关（参考保存的记忆 / 参考会话上下文）按 owner 持久化，在记忆装配唯一出口门控注入；补长期事实清单只读接口供前端管理面板。明确不实现前端 UI（拆 S-031b）/ 单条事实删除（推迟 v1.1） | Done | `domain/models.py`（+`MemorySettings` + `AuditAction.MEMORY_SETTINGS_UPDATE`）, `domain/ports.py`（+`MemorySettingsStorePort` + `MemoryPort.list_facts`）, `infra/storage/_db.py`（+`memory_settings` 表 PK owner_id）, **新增** `infra/storage/sqlite_memory_settings.py:SqliteMemorySettingsStore`, `infra/memory/task_memory.py`（+`list_facts` 过滤 superseded/TTL 倒序）, `app/memory/assembler.py`（+`settings_store` 门控 + `_safe_settings` fail-open）, **新增** `app/use_cases/memory_settings.py:MemorySettingsUseCase`（get/update + 同意审计）, `app/factories.py`（+`build_memory_settings_store`）, `app/container.py`（装配 settings_store + memory_settings）, `api/v2/{schemas,memory}.py`（+settings/facts DTO + `GET/PUT /memory/settings` + `GET /memory/facts`）, **新增** `tests/fakes/fake_memory_settings_store.py` + 开关/门控/事实清单/API 多类测试 | `pytest`（默认）/ `import main` live / `ruff check` | 全量 **771 passed / 1 skipped**（较 030d +33）；ruff All checks passed（`ports.py` 导入 `--fix` 归位）；`import main` OK，`memory_settings`/`memory_settings_store` 正常装配，`/api/v2/memory/*` 路由正常挂载 | (1) 开关每用户运行时偏好独立表不进 config（D1）/ (2) use_saved_memory→L3+L4、reference_history→L1+L2 正交映射（D2）/ (3) 门控放 MemoryAssembler 唯一出口 copilot 零改动（D3）/ (4) 缺省双开 + 读取异常 fail-open（D4）/ (5) 诚实标签「参考会话上下文」非「历史聊天记录」（D5）/ (6) 开关变更落 MEMORY_SETTINGS_UPDATE 同意审计只记最终值（D6）/ (7) 管理面板复用 /profile + 新 /facts，单条删除推迟 v1.1（D7）/ (8) list_facts 过滤口径同 recall_semantic（D8）/ (9) API owner 隔离 + memory 禁用降级 200（D9） | Step 031b 前端设置弹窗 + 双 toggle + 管理面板 / 单条事实删除 v1.1 / 跨会话历史召回 |
| [031b](step-031b_memory_settings_ui.md) | **记忆与隐私设置（前端 UI）**：S-031a 后端能力的前端闭环。账户菜单加「⚙ 记忆与隐私」入口，打开模态——两个 iOS 风开关（参考保存的记忆 / 参考会话上下文）乐观切换、已保存记忆只读管理面板（用户画像 + 长期事实 N/cap）、被遗忘权两个清除按钮（清空记忆 / 清空全部含对话）。纯静态前端零构建依赖 | Done | **新增** `frontend/settings.js`（模态控制器 mount/open/close + 乐观切换 + 画像/事实加载 + 清除二次确认 + `onMemoryCleared`）, `frontend/api.js`（+`memory` 客户端 profile/getSettings/updateSettings/facts/forget）, `frontend/index.html`（菜单项 + 模态骨架）, `frontend/app.js`（绑入口 + 订阅 onMemoryCleared）, `frontend/style.css`（+`.modal`/`.switch`/`.memory-list`/`.btn-danger`） | 浏览器实测（匿名会话 `127.0.0.1:8765`） | 菜单项→模态正常；双开关默认 ON、画像 0、事实 0/500、危险区两按钮，暗色皮肤一致；翻「参考会话上下文」→「已保存」且 `GET /settings` 实测 `reference_history:false` 持久化；✕/遮罩/Esc 关闭 aria 同步；无控制台报错；后端纯前端未改→S-031a 基线 771 passed/1 skipped | (1) 独立覆盖式模态不复用 switchView（D1）/ (2) 乐观切换 + 失败回滚（D2）/ (3) 每 toggle 只 PUT 自身字段借 None 语义防并发覆盖（D3）/ (4) 被遗忘权强二次确认 + 文案区分保留/连带删对话（D4）/ (5) scope=all 经 emitCleared 联动 app 刷任务+重置对话保持单向数据流（D5）/ (6) 匿名也有 owner 故菜单常驻（D6）/ (7) 诚实空态 + N/cap 容量预期（D7）/ (8) 零构建依赖沿用 vanilla ESM + :root token（D8） | 单条事实/画像删除 v1.1（需后端 DELETE）/ 开关在主聊天界面的可视化提示 / 跨会话历史召回前端呈现 |

## 写作规范

- **文件名**：`step_NNN_<snake_topic>.md`，NNN 从 001 起递增、零填充三位
- **每步必含小节**（参考 phonepilot `docs/process/step_001_core_schema.md` 模板）：
  1. 本步骤目标（为什么存在 / 服务于哪层 / 为后续提供什么）
  2. 修改文件（精确路径 + 一句话说明）
  3. 设计决策（关键技术选择 + 替代方案）
  4. 核心契约 / 接口
  5. 与外部服务的关系
  6. 当前实现范围（已实现 / 未实现按设计）
  7. 暂未实现 / TODO
  8. 测试与验证（命令 + 输出）
- **更新 README 大表**：每完成一个 step 追加一行；Status / Result / Conclusion / Next 必填
- **不事后美化**：失败的尝试、走错的路也要写
- **不复述代码**：贴文件路径与命令即可，diff 以 git 为准

## 原则

- 一个用户 prompt 触发的一段实际工作 ≈ 一个 step
- 文档 / ADR / 设计冻结也算 step（无代码可省略 §4 §8 但其他小节齐全）
- 大表是入口，正文是细节，二者必须一致
